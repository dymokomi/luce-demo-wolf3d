# Shareware data

Apogee Wolfenstein 3D 1.4 shareware (`.WL1`) files used at runtime. An install
script will copy these next to the binary on other machines later.

`WOLF3D.EXE` is the original DOS shareware executable, kept with the set.
